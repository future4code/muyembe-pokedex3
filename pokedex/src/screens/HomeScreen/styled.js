import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
  homeGridContainer: {
    paddingTop: "20px",
    paddingLeft: "60px",
    paddingRight: "60px",
  },

  homeScreen: {
    textAlign: "center",
    marginTop: "40px",
    minHeight: "100vh",
  },
}));
