import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
  footer: {
    position: "static",
    top: "auto",
    bottom: 0,
    color: "primary",
    height: "80px",
  },
}));
