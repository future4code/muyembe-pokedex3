import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
  root: {
    paddingTop: "60px",
    textAlign: "center",
    alignItems: "center",
  },

  Img: {
    width: "auto",
    height: "400px",
  },
}));
